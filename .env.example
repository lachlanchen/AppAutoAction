# Copy to .env and keep the real file untracked.
# Use either NPM_TOKEN or NODE_AUTH_TOKEN. Both may point to the same npm token.
NPM_TOKEN=
NODE_AUTH_TOKEN=
NPM_CONFIG_REGISTRY=https://registry.npmjs.org/
